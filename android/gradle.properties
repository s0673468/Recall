org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Recall has not migrated every Android plugin to AGP's new DSL yet.
android.newDsl=false
# AGP 9.2.1 embeds Kotlin 2.2.10, below Flutter 3.47's minimum 2.2.20.
# Keep the standalone Kotlin plugin until AGP's embedded compiler catches up.
android.builtInKotlin=false
